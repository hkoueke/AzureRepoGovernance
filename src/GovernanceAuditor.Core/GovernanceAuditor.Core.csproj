<Project Sdk="Microsoft.NET.Sdk">

  <!--
    GovernanceAuditor.Core : domaine + contrats + analyseurs.
    RÈGLE D'ARCHITECTURE : ce projet ne référence AUCUNE infrastructure,
    aucun client HTTP, aucun SDK Azure DevOps. Uniquement des abstractions.
    Cette règle est vérifiée par un test (voir tests/.../Architecture).
  -->
  <PropertyGroup>
    <RootNamespace>GovernanceAuditor.Core</RootNamespace>
    <AssemblyName>GovernanceAuditor.Core</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Options.DataAnnotations" />
  </ItemGroup>

</Project>
